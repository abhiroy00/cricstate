# Used by docker-compose.yml for the postgres service and to build backend's
# DATABASE_URL inside the compose network. Copy to .env before `docker compose up`.
POSTGRES_USER=cricstate
POSTGRES_PASSWORD=cricstate
POSTGRES_DB=cricstate
