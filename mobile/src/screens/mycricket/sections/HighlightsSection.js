import EmptyState from "../../../components/EmptyState";

export default function HighlightsSection() {
  return (
    <EmptyState
      title="Live stream your matches"
      subtitle="Let AI automatically create your best moments. This is coming in a later phase, once live video is built."
    />
  );
}
