# Mobile currently reads config from src/utils/config.js (see comment there).
# This file documents what's needed once react-native-config is introduced,
# and what native files Firebase needs dropped into the project:
#
# API_BASE_URL=http://10.0.2.2:8000/api/v1
#
# Firebase Cloud Messaging (see README "Mobile setup"):
#   android/app/google-services.json
#   ios/GoogleService-Info.plist
